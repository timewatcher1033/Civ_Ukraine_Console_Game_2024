1."""...""": Це багаторядковий рядок документації, який пояснює, що робить функція.


2.try:
    from clock import display
    # Основний код тут
except KeyboardInterrupt:
    print("Програму перервано користувачем.")
except ImportError as e:
    print(f"Помилка імпорту: {e}")

3.Новий рядок посеред print'у
    print("Маленький маленький павук \n піднявся на водограй.")

4. sep розділювач рядків коли передаєш параметри
    print("Мене", "звати", "Монті", "Пайтон.", sep="=")
Мене=звати=Монті=Пайтон.